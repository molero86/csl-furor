# ============================================
# CONFIGURACIÓN DE PRODUCCIÓN - FUROR APP
# ============================================
# 
# IMPORTANTE: Este archivo NO debe subirse a git
# Copia este archivo como .env y completa con tus valores reales

# URL del backend (debe ser accesible desde internet)
# Usa HTTPS en producción, HTTP solo para desarrollo local
# NO uses localhost ni 127.0.0.1 si quieres acceso externo
VITE_API_URL=https://api.furor.molero.org

# Configuración de base de datos MySQL
# Formato: mysql+pymysql://usuario:contraseña@host:puerto/nombre_base_datos
# 
# Ejemplos:
# - Base de datos local: mysql+pymysql://root:password@localhost:3306/furor
# - Desde Docker a host: mysql+pymysql://user:pass@host.docker.internal:3306/furor
# - Base de datos remota: mysql+pymysql://user:pass@db.ejemplo.com:3306/furor
DATABASE_URL=mysql+pymysql://usuario:contraseña@host:3306/furor

# Orígenes permitidos para CORS (separados por comas, sin espacios)
# Incluye TODOS los dominios desde los que se accederá a la app
# 
# IMPORTANTE: Incluye tanto http como https, www y sin www
CORS_ORIGINS=https://furor.molero.org,https://www.furor.molero.org,http://localhost:4018,http://localhost:5173

# ============================================
# NOTAS IMPORTANTES
# ============================================
#
# 1. Para desarrollo local:
#    VITE_API_URL=http://localhost:4017
#    DATABASE_URL=mysql+pymysql://root:root@localhost:3306/furor
#    CORS_ORIGINS=http://localhost:5173,http://localhost:4018
#
# 2. Para producción:
#    - Usa HTTPS en VITE_API_URL
#    - Asegúrate de que el backend sea accesible públicamente
#    - Incluye todos tus dominios en CORS_ORIGINS
#    - Verifica que la base de datos sea accesible desde Docker
#
# 3. Después de cambiar este archivo:
#    docker-compose down
#    docker-compose build --no-cache
#    docker-compose up -d
#
# 4. Para verificar que se aplicó:
#    docker exec furor_frontend cat /usr/share/nginx/html/env.js
#    docker exec furor_backend printenv | grep CORS
